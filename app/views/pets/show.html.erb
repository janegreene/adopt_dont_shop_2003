<%= image_tag @pet.image, id: "pet-avatar-#{@pet.id}"%>
<h1> <%= @pet.name %> </h1>
  <h2><%= @pet.description %></h2>
  <h2><%= @pet.approximate_age %></h2>
  <h2><%= @pet.adoption_status %></h2>
  <h2><%= link_to "Update Pet", "/pets/#{@pet.id}/edit"%></h2>
<%= button_to "Delete Pet", "/pets/#{@pet.id}", method: :delete %>
