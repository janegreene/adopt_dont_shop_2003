<nav>
<ul>
    <li><a href="/pets">All Pets</a></li>
    <li><a href="/shelters">All Shelters</a></li>
</ul>
</nav>
<h1>All Shelters</h1>

<% @shelters.each do |shelter| %>
  <h2><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %></h2>
  <h2><%= link_to "Edit #{shelter.name}", "/shelters/#{shelter.id}/edit"%></h2>
  <h2><%= link_to "Delete #{shelter.name}", "/shelters/#{shelter.id}/delete"%></h2>
<% end %>
<a href="/shelters/new">New Shelter</a>
